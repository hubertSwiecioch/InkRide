<?xml version="1.0" encoding="utf-8"?>
<!--
    The runtime BLE permissions live in :feature:ble:data, which this module
    does not depend on. BleSensorsRoot requests them from a LaunchedEffect on
    API 31+, and GrantPermissionRule can only grant a permission the test APK
    declares — without this, the request would be denied out of hand and the
    grant would fail.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
</manifest>

# Pinned to API 33: BLUETOOTH_CONNECT enforcement (and ShadowBluetoothDevice's
# security-exception simulation) only applies from API 31+. Robolectric
# silently defaults a library module (no targetSdkVersion in its merged
# manifest) to API 23, which predates that entirely -- see the identical note
# in :feature:tracking:data's robolectric.properties.
sdk=33
